@{
    Layout = "/_Page.cshtml";
    var posts = Metadata.Get<IEnumerable<IDocument>>("Posts");
}

<ul class="list-posts">
    @foreach(var doc in posts.OrderByDescending(x => x["PostFile"]))
    {
        @Html.Partial("_PostTeaser", doc)
    }

    @RenderBody()
</ul>